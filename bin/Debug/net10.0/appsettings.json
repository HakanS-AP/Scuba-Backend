{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AdminSettings": {
    // Key is intentionally blank here.
    // Development: set it in appsettings.Development.json (never commit a real key).
    // Production:  use an environment variable named AdminSettings__Key,
    //              Azure App Configuration, or Azure Key Vault.
    "Key": ""
  }
}
